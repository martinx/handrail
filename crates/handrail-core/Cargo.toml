[package]
name = "handrail-core"
version = "0.1.0"
description = "Handrail core: pack catalog, validation, file plans and transactional apply."
edition.workspace = true
license.workspace = true
repository.workspace = true
publish = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
sha2 = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
